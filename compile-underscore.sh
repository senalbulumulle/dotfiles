## compile-underscore.sh

cp -R _fish fish
cp -R _i3 i3
cp -R _sway sway
cp -R _kitty kitty


## Then compile the thing to the .config directory
mv fish /Users/$USER/.config/
mv kitty /Users/$USER/.config/
